---
title: "Lc15_三数之和"
date: 2022-06-02T23:44:04+08:00
draft: true
tags:
  - "双指针"
  - "medium"
author: ["zzydev"]
---

```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> res;
        sort(nums.begin(), nums.end());
        int n = nums.size();
        if (n < 3) return res;
        for (int i = 0; i < n - 2; i ++) {
        //------------------------双指针：这部分维度降为O(n)---------------------
            if (i && nums[i] == nums[i - 1]) continue; //跳过相同的nums[i]
            for (int j = i + 1, k = n - 1; j < k; j ++) {
								//跳过相同的nums[j] 注意 j > i + 1
                if (j > i + 1 && nums[j] == nums[j - 1]) continue;
								//注意 j < k - 1，使用nums[k - 1]试探，如果大于等于0，说明结果至少是nums[k --]
                while (j < k - 1 && nums[i] + nums[j] + nums[k - 1] >= 0) k --;
                if (nums[i] + nums[j] + nums[k] == 0) res.push_back({nums[i], nums[j], nums[k]});
            }
       //---------------------------------------------------------------------
        }
        return res;
    }
};
```
[lc16_最接近的三数之和]({{< ref "./lc16_最接近的三数之和.md" >}} )   
[lc01_两数之和]({{<ref "./lc01_两数之和.md">}})   
[lc18_四数之和]({{<ref "./lc18_四数之和.md">}})